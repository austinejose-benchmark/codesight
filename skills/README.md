# Skills (future)

Drop a skill here as `skills/<name>/SKILL.md`. Claude Code auto-discovers it
once the plugin is installed. Example use: a `/codesight-explain` skill that reads
`.codesight/` and answers questions about the map.

Nothing here yet — this folder is a placeholder so the plugin is ready to grow.
